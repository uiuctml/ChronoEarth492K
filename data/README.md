# Data Directory

Place local datasets under this directory when running training or evaluation scripts, or pass an explicit `--data_dir` argument. Generated metadata, model checkpoints, experiment outputs, and symlinks to private storage are intentionally ignored.
